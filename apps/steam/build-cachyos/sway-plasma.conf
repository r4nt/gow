# Minimal sway config for nested KDE Plasma session.
# Sway is used purely as a compositor host — no keybindings, no decorations, no bar.
# Resolution is prepended at runtime by startup.sh.

default_border none
default_floating_border none

# Exit sway when the Plasma session ends.
exec /opt/gow/kde-startup.sh
